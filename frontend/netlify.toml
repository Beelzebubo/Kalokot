[build]
  command = "npm run build"
  publish = "dist"
  functions = ".netlify/functions-internal"

# API proxy — routes /api/* requests to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://kalokot-backend.onrender.com/api/:splat"
  status = 200
